package com.multi_spring_configurable_context.b;

import net.rgielen.fxweaver.core.FxmlView;
import org.springframework.stereotype.Controller;

@Controller
@FxmlView("/BView.fxml")
public class BController {

    // B controller, no changes required.
}